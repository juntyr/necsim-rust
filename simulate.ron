(
    speciation: 0.1,
    seed: 24,
    sample: 1.0,

    algorithm: SkippingGillespie(
        parallelism_mode: Optimistic(delta_sync: 0.01),
    ),

    log: "event_log",

    scenario: AlmostInfinite(
        radius: 1000,
        sigma: 1.0,
    )
)
