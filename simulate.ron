(
    speciation: 0.1,
    seed: 24,
    sample: 1.0,

    algorithm: SkippingGillespie(
        partition_mode: Lockstep,
    ),

    log: "event_log",

    scenario: AlmostInfinite(
        radius: 1000,
        sigma: 1.0,
    )
)
