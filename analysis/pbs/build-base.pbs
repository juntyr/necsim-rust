#PBS -lselect=1:ncpus=1:mem=16gb
#PBS -lwalltime=01:00:00

cd $HOME/necsim-rust

./setup.sh

$HOME/.cargo/bin/cargo run --release --target-dir target-replay

$HOME/.cargo/bin/cargo run --release --features rustcoalescence-algorithms-monolithic,rustcoalescence-algorithms-independent --target-dir target-base
