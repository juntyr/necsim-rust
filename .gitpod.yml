
image:
  file: .gitpod.Dockerfile

tasks:
  - init: cargo install ptx-linker -f && cargo install cargo-mpirun -f && cargo install cargo-post -f && ./setup.sh

vscode:
  extensions:
    - bungcip.better-toml
    - serayuzgur.crates
    - a5huynh.vscode-ron
    - matklad.rust-analyzer
